{% extends '/layouts/base.html.twig' %}

{% block title 'Property index'%}

{% block body %}

<h1>Liste des ...</h1>

{% for property in properties %}
    <h2>{{ property.title }}</h2>
    {% if property.images is not empty %}
        <img width="200" src="{{ vich_uploader_asset(property.images.0) }}" alt="">
    {% endif %}
{% endfor %}

{% endblock %}
